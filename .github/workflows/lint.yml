name: Lint

on:
  push:
  pull_request:

jobs:
  lint:
    name: Add-on linter
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Lint Threadfin add-on
        uses: frenck/action-addon-linter@v2
        with:
          path: "./threadfin"
